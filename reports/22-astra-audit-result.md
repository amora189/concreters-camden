# Stage 22 Astra audit result

Generated by `scripts/22-astra-audit.py`. Fail-closed: a partial export fails here,
not after import.

```text
  candidate files           1
  parsed as Astra exports   1
  required mod groups       1
  groups present            1
  design-carriage           PASS
  internal-consistency      PASS
  total failures            0
  verdict                   PASS
```

Revised 19 August 2026 per `reports/42-astra-vs-elementor-design-carriage.md` §4. The six
design groups are REPORTED, not required: a Customizer export stores only what was
explicitly set, so UNSET is a valid stock configuration rather than a partial export.

## Required mod groups

```text
  site-identity    PRESENT  custom_logo, site_icon
```

## Required — design carriage

```text
  PASS  resolves from: Elementor kit (4 colours, 4 type entries); page _elementor_data (3936 colour literals, 2655 type entries)  [!] the design is carried in PAGE DATA, not in a governing layer — the site cannot be restyled centrally
```

## Required — internal consistency

```text
  PASS  every attachment ID, menu term and custom_css_post_id the export references exists in the WXR
```

## Reported mod groups (not required)

```text
  colours          UNSET    never customised — valid stock configuration
  typography       UNSET    never customised — valid stock configuration
  layout           UNSET    never customised — valid stock configuration
  header           UNSET    never customised — valid stock configuration
  footer           UNSET    never customised — valid stock configuration
  buttons          UNSET    never customised — valid stock configuration
```

## Parse findings

```text
  astra-export.dat: parsed as PHP-serialised Customizer export (.dat), 22 keys
```
